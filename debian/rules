#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ 

override_dh_auto_install:
	$(MAKE) PREFIX=$$(pwd)/debian/l3overlay SBIN_DIR=$$(pwd)/debian/l3overlay/usr/sbin install
