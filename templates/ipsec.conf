# {{ file }}
# This file was automatically generated by l3overlayd.

{% for mesh_tunnel_link in mesh_tunnel_links %}
conn {{ mesh_tunnel_link[0] }}-{{ mesh_tunnel_link[1] }}
  keyexchange = ikev2
  ike = aes256-sha512-ecp384
  esp = aes256-sha512-ecp384
  ikelifetime = 14400s
  lifetime = 3600s
  dpdaction = restart
  keyingtries = %forever
  authby = secret
  left = {{ mesh_tunnel_link[0] }}
  right = {{ mesh_tunnel_link[1] }}
  leftsubnet = {{ mesh_tunnel_link[0] }}[gre]
  rightsubnet = {{ mesh_tunnel_link[1] }}[gre]
  type = transport
  auto = route

{% endfor %}
